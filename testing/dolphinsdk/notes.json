{
  "gxdemo__G2D-test": {
    "note": "Runs (the OSReport intro is printed and the TPL is read from the disc), but the picture is a flat dark shape: the G2D scene (tiles, ship sprite, shadow) is missing. The demo is the only one in the set that writes the CP statistics register CP_STAT_SEL (0x20) once per frame (196 times in 7 s), which suggests the frame loop is running but the G2D layer drawing is not producing output.",
    "analysis": "pe finishes: 0 - the frame counter does not apply here. vi: 742. CP 0x20: 196.\nSource: G2D/G2D-test.c loads gxTests/G2D-00.tpl from the disc (1 441 952 bytes, the log shows exactly three disc reads of that size), then G2DDrawLayer/G2DDrawSprite per frame.\nThe emulator log shows the TPL being read, the XF and TEV shaders compiling and a \"Unknown CP load, index: 0x20, data: 0x00000000\" every frame (CP_STAT_SEL, the GX performance-counter select - 0x20 is (0x2 << 4) | 0 in the CP command-stream address encoding, see command-processor specs 5.1).\nOpen: why the layer draw produces no visible geometry.\n\nEvidence from the draw-command log (one run per demo):\n  - tev-outline submits 3402 primitives and 378 display-list calls in 3 s, so the model and the\n    display list do reach the command processor - the picture (one colour, the demo's own\n    clear colour 160,160,160) is not a missing draw, it is state: the model colour comes from\n    the per-vertex colour (GX_SRC_VTX) and the per-vertex position-matrix index, and the\n    enlarged black cartoon outline is missing too.\n  - ind-bump-xyz / ind-bump-st submit their donut (1900 GX_QUADS of 4 vertices plus strips) and\n    still come out pure black. Their textures come from gxTests/dnt-03.tpl and dnt-02.tpl, both\n    of which exist on the virtual disc, and the same TPL loading path works in tex-fmt-tpl.\n  - G2D-test submits a single draw per frame (GX_QUADS, 472 vertices = 118 quads, about 32/s),\n    which is the 2D layer batch; the captured picture is that layer drawn huge and dark\n    rather than the expected game scene.\nThese three are the next cases to work: they all have geometry, so the fault is in the state\nthat turns it into pixels (the per-vertex matrix index and the indirect/bump stage)."
  },
  "gxdemo__lit-atn-func": {
    "note": "Two colours only: a black shape on the grey background. The demo walks through 11 light-attenuation functions and should show a shaded surface, so the black object suggests the attenuation lookup is not applied. CP 0x20: 197 (once per frame).",
    "analysis": "pe finishes: 0 (the GXDrawDone counter does not apply to this run). vi: 728.\nSource: Lighting/lit-atn-func.c - one object, the attenuation function is stepped with the A button, the intro says \"to quit hit the start button\".\nThe shape is drawn (the silhouette is correct) but it is unlit black on the grey clear colour."
  },
  "gxdemo__tex-invalid": {
    "note": "CP 0x20 is written 175 times (once per row of texture updates per frame); the picture has some content but the demo is the invalid-texture stress test, so a per-case comparison against the source is needed.",
    "analysis": "pe finishes: 0. vi: 777.\nSource: Texture/tex-invalid.c - after each row of quads it calls GXSetDrawSync(++token) and spins on GXReadDrawSync(); it also uses GXInvalidateTexRegion/GXInvalidateTexAll between rows. The GXReadDrawSync/GXSetDrawSync path is exercised here more than anywhere else in the set."
  },
  "gxdemo__mgt-fifo-brkpt": {
    "note": "FIFO break-point demo: it arms the CP break point and stops the FIFO on purpose, so the flat green screen and the missing frame counter are expected behaviour, not a failure.",
    "analysis": "pe finishes: 0, vi: 64 (the shortest run in the set).\nSource: Management/mgt-fifo-brkpt.c - the demo is about CP_FIFO_BRKL/BRKH and the CP status BRK bit (command-processor specs 4.2)."
  },
  "gxdemo__mgt-triple-buf": {
    "note": "Triple-buffer management demo; the picture has two colours and no frame was counted, which fits a demo that deliberately switches between three XFBs and waits on draw sync.",
    "analysis": "pe finishes: 0, vi: 78.\nSource: Management/mgt-triple-buf.c - drives three frame buffers through VI and uses the draw-sync token to know when a buffer is free."
  },
  "gxdemo__perf-geo": {
    "note": "A performance measurement demo (green screen). It is meant to be timed, not looked at, so the flat picture is not a rendering failure.",
    "analysis": "pe finishes: 0, vi: 775. CP 0x20: 0.\nSource: Performance/perf-geo.c - geometry throughput measurement."
  },
  "gxdemo__DL-tf-mtx": {
    "note": "FIXED by this sweep: the display-list model renders now (179 colours; before it was a flat grey screen). The demo loads one matrix per contour with GXLoadPosMtxIndx/GXLoadTexMtxIndx, which the emulator ignored.",
    "analysis": "Before: one colour (64,64,64). After: 179 colours, 71% background.\nThe model itself is still not fully right - the remaining picture is dominated by large flat areas - so the demo is a useful follow-up case for the texture-matrix side of the same path.\nRoot cause (fixed in src/cp.cpp): the 0x20..0x3F commands are XF_IndexLoadRegA..D, block loads of the XF matrix/light area whose source address comes from one of the four index arrays (A..D = CP arrays 0xC..0xF). The emulator only logged them, so the matrices never reached the XF."
  },
  "gxdemo__frb-bound-box": {
    "note": "FIXED by this sweep: the demo shows its eight coloured cubes now. The cubes and the bounding-box outline were reaching the command processor all along; the picture was lost because the PE draw-sync token was treated as a frame boundary, so the frame was swapped half-way through and the buffer the display copy presented held only the (degenerate) outline. The white outline is still missing: the emulator does not track the bounding box yet.",
    "analysis": "Before the fix: pure black (one colour). After: 46 colours, 82% background, the eight GXDrawCube\ncubes visible in their material colours.\n\nRoot cause (fixed in src/pe.cpp): the PE token path called GPFrameDone(), which presents the frame.\nfrb-bound-box puts GXSetDrawSync in the middle of the frame - draw the cubes, sync, wait, read the\nbounding box back, draw the outline - so the swap happened with only the cubes drawn, and the\ndisplay copy at the end of the frame then presented the other back buffer, which held just the\noutline over nothing. A draw-sync token is a synchronisation marker, not the end of a frame; the\nframe is presented at GXDrawDone (PE_FINISH) and at a full-frame display copy (GPDisplayCopy),\nwhich are the points where the picture is complete.\n\nEvidence gathered before the fix:\n  - CP draw log: 2728 x \"Draw: cmd 0x83, vtxnum 24, vat 3\" (the cubes) and 341 x \"Draw: cmd 0xB0,\n    vtxnum 5, vat 0\" (the outline) - the geometry did reach the command processor.\n  - XF lighting log: ambient 0x404040FF, the eight cube materials and channel control 0x00000506\n    (lighting on, light 0, N.L clamped, attenuation off), exactly what the demo asks for.\n  - Forcing the illumination term to 1.0 in the vertex shader still gave black, which ruled the\n    lighting arithmetic out and pointed at the presentation path.\n\nStill open: the bounding box itself. PE_XBOUND/YBOUND (0x55/0x56) latch the BP write but are never\nupdated from the drawn geometry, and the CPU-visible PE_PI_XBOUND0/1 and YBOUND0/1 pair\n(0x10/0x12/0x14/0x16) reads back as 0, so GXReadBoundingBox always answers (0,0,0,0) and the white\noutline collapses to a point (gfx-pe 6.17 and gfx-pe 8)."
  },
  "gxdemo__ind-bump-st": {
    "note": "Flat black: the indirect bump-mapping demo draws nothing. It uses GXSetTevIndBumpST with the indirect matrix GX_ITM_0 plus a normal-perturbation texture in TEXMAP2.",
    "analysis": "pe: 872, vi: 881, shot: one colour (0,0,0).\nSource: Indirect/ind-bump-st.c - renderSetup2() calls GXSetTevIndBumpST(GX_TEVSTAGE1, GX_INDTEXSTAGE0, GX_ITM_0); the map file confirms GXSetTevIndBumpST is linked in, so the #ifndef EMU blocks in the demo are not the cause. The demo also calls GXSetIndTexMtx.\nThe plain indirect demos (ind-pseudo-3d, ind-tile-test, ind-warp) do render, so the difference is the bump matrix mode (GX_ITM_0/1/2) rather than indirect texturing as such.\n\nEvidence from the draw-command log (one run per demo):\n  - tev-outline submits 3402 primitives and 378 display-list calls in 3 s, so the model and the\n    display list do reach the command processor - the picture (one colour, the demo's own\n    clear colour 160,160,160) is not a missing draw, it is state: the model colour comes from\n    the per-vertex colour (GX_SRC_VTX) and the per-vertex position-matrix index, and the\n    enlarged black cartoon outline is missing too.\n  - ind-bump-xyz / ind-bump-st submit their donut (1900 GX_QUADS of 4 vertices plus strips) and\n    still come out pure black. Their textures come from gxTests/dnt-03.tpl and dnt-02.tpl, both\n    of which exist on the virtual disc, and the same TPL loading path works in tex-fmt-tpl.\n  - G2D-test submits a single draw per frame (GX_QUADS, 472 vertices = 118 quads, about 32/s),\n    which is the 2D layer batch; the captured picture is that layer drawn huge and dark\n    rather than the expected game scene.\nThese three are the next cases to work: they all have geometry, so the fault is in the state\nthat turns it into pixels (the per-vertex matrix index and the indirect/bump stage).\n\nNarrowed down by experiment: forcing the texture sampler to return white for every map makes the donut appear (2 colours, 16% of the screen), so the geometry, the projection, the TEV structure and the indirect stage all work - it is the texture *values* that are black. Forcing white for map 1 only (TEXMAP1, the light map) reproduces it exactly, while forcing map 0 or 2 changes nothing, so the light map is the black one.\nThe light map is generated once before the frame loop: lightMapSetup() renders a hemisphere into the EFB with lighting on (diffuse in COLOR0, specular in ALPHA0) and GXCopyTex(lightMap, FALSE) copies it into an IA8 texture in main memory. The demo also calls showMaps() every frame to display that light map, and nothing of it is visible either, so the texture really is empty.\nNext: instrument which side is empty - the hemisphere render (the light and the two channel controls) or the IA8 texture copy (the emulator does implement TF_IA8 in TextureCopy, and its byte order matches the texture decoder in tx.cpp, so the copy path itself looks consistent).\n\nMore evidence (all of it from temporary logs and shader experiments, reverted afterwards):\n  - The copy does run: a TEXCOPY log in PixelEngine::TextureCopy reports exactly one copy for this demo,\n    64x64, fmt=3 (IA8), source (0,0), destination 0x00239F40, a valid main-memory pointer, stride 16.\n    The working frb-copy demo copies 32x32 fmt=4 (RGB565) 354 times through the same code, so the copy\n    engine itself is fine.\n  - The light is a standard specular setup: GXInitSpecularDir + GXInitLightColor(0x88) +\n    GXInitLightAttn(-3, 0, 4, ...) with the comment \"Light equation 4x^2 - 3, x = N.H\". The emulator's\n    CosineAttenuation computes exactly clamp(a0 + a1*cos + a2*cos^2) = clamp(-3 + 4*cos^2), and the\n    diffuse material is white, so with the illumination term forced to 1.0 the hemisphere must render\n    white.\n  - Forcing the illumination term to 1.0 does NOT make the donut appear, so the lighting of the\n    hemisphere is not the fault: the hemisphere render, the copy and the texture the TEV samples do not\n    meet. The next thing to check is that the address the copy writes (PE_COPY_DST_BASE << 5, a physical\n    address) and the data pointer of the texture object the TEV samples (a pointer the emulator masks\n    itself) really are the same memory.\n\nDecisive check of the addressing: a temporary log of the texture engine's image address shows the\nlight map is programmed at addr=0x00239F40, fmt=3 (IA8), 64x64, 8192 bytes - exactly the destination\nthe copy wrote (0x00239F40, fmt=3, 64x64, stride 16). So the copy writes precisely where the sampler\nreads, and the light map is empty only because the EFB the copy read back held nothing.\nThe render that should fill it is drawHemisphere() (triangle strips over the whole screen with an\northographic projection, white diffuse material, the specular light above), and forcing the\nillumination term to 1.0 still left the picture black - which points at the ordering rather than at\nthe values: the GXCopyTex reads the EFB back, and the geometry the demo drew just before it has to be\nin the GL framebuffer by then. Next: check the emulator's GL draw batching/flush order around\nPE_COPY_CMD, i.e. whether ReadEfb sees the draws that the CP issued earlier in the same frame.\n\nFinal narrowing of this pass: the copy's readback *succeeds* - the TEXCOPY log line sits after the\nReadEfb() call and TextureCopy returns early when the readback fails - so the GL context is current,\nglReadPixels runs, and it returns black. The hemisphere the demo drew immediately before the copy is\ntherefore not in the framebuffer yet: the emulator clears/starts the GL frame on the draw path\n(GPFrameBegin -> GL_BeginFrame) while PE_COPY_CMD reads the EFB back, so the order of start-frame and\nreadback around the first frame is what to look at next (the demo does its one and only light map\nrender plus GXCopyTex *before* the main loop, i.e. before the first DEMOBeforeRender).\n\nAlso ruled out: the light map render targets the top-left 64x64 of the EFB\n(setupSize -> GXSetViewport(0,0,64,64) + GXSetScissor(0,0,64,64)), and TextureCopy converts that\nrectangle correctly for the GL readback (ReadEfb(srcX, RenderHeight - srcY - h, ...)), so the\nviewport placement is not the reason the copied rectangle is empty. That leaves the frame order:\nGPFrameBegin()/GL_BeginFrame() (which clears) runs from the draw path in cp.cpp, while PE_COPY_CMD\nreads the EFB back - for a copy made before the main loop's first frame the two have to be checked\nagainst each other.\n\nAnd the viewport mapping itself is right: GL_SetViewport does glViewport(x, scr_h - (h + y), w, h), i.e.\nit flips GX's top-left origin for GL, and RAS_End() issues glDrawArrays synchronously at GXEnd, so the\nhemisphere really is drawn into the GL buffer before the copy command is walked. What is left for this\ndemo is the frame bookkeeping (GPFrameBegin/GL_BeginFrame and the pending copy clears) around a\nPE_COPY_CMD that arrives before the main loop's first frame.\n\nOne more hypothesis ruled out: GL_SetScissor documents and implements the Y flip for the scissor\nrectangle (\"the programmed scissor rectangle is in screen coordinates, the origin is the top left\ncorner of the EFB ... GL measures its scissor box from the bottom left corner, so the Y axis is\nflipped here\"), so the 64x64 scissor and the 64x64 viewport agree with the rectangle the copy reads.\nWith the geometry, the lighting, the addressing, the viewport, the scissor and the GL draw order all\nchecked, what is left is the frame bookkeeping (GPFrameBegin/GL_BeginFrame plus the pending copy\nclears) around a PE_COPY_CMD that arrives before the main loop's first frame."
  },
  "gxdemo__ind-bump-xyz": {
    "note": "Same failure as ind-bump-st: flat black. This one uses GXSetTevIndBumpXYZ.",
    "analysis": "pe: 859, vi: 867, shot: one colour (0,0,0).\nSource: Indirect/ind-bump-xyz.c - renderSetup2() calls GXSetTevIndBumpXYZ(GX_TEVSTAGE1, GX_INDTEXSTAGE0, GX_ITM_0) and GXSetIndTexMtx; GXSetTevIndBumpXYZ is linked in (map file).\n\nEvidence from the draw-command log (one run per demo):\n  - tev-outline submits 3402 primitives and 378 display-list calls in 3 s, so the model and the\n    display list do reach the command processor - the picture (one colour, the demo's own\n    clear colour 160,160,160) is not a missing draw, it is state: the model colour comes from\n    the per-vertex colour (GX_SRC_VTX) and the per-vertex position-matrix index, and the\n    enlarged black cartoon outline is missing too.\n  - ind-bump-xyz / ind-bump-st submit their donut (1900 GX_QUADS of 4 vertices plus strips) and\n    still come out pure black. Their textures come from gxTests/dnt-03.tpl and dnt-02.tpl, both\n    of which exist on the virtual disc, and the same TPL loading path works in tex-fmt-tpl.\n  - G2D-test submits a single draw per frame (GX_QUADS, 472 vertices = 118 quads, about 32/s),\n    which is the 2D layer batch; the captured picture is that layer drawn huge and dark\n    rather than the expected game scene.\nThese three are the next cases to work: they all have geometry, so the fault is in the state\nthat turns it into pixels (the per-vertex matrix index and the indirect/bump stage).\n\nNarrowed down by experiment: forcing the texture sampler to return white for every map makes the donut appear (2 colours, 16% of the screen), so the geometry, the projection, the TEV structure and the indirect stage all work - it is the texture *values* that are black. Forcing white for map 1 only (TEXMAP1, the light map) reproduces it exactly, while forcing map 0 or 2 changes nothing, so the light map is the black one.\nThe light map is generated once before the frame loop: lightMapSetup() renders a hemisphere into the EFB with lighting on (diffuse in COLOR0, specular in ALPHA0) and GXCopyTex(lightMap, FALSE) copies it into an IA8 texture in main memory. The demo also calls showMaps() every frame to display that light map, and nothing of it is visible either, so the texture really is empty.\nNext: instrument which side is empty - the hemisphere render (the light and the two channel controls) or the IA8 texture copy (the emulator does implement TF_IA8 in TextureCopy, and its byte order matches the texture decoder in tx.cpp, so the copy path itself looks consistent).\n\nMore evidence (all of it from temporary logs and shader experiments, reverted afterwards):\n  - The copy does run: a TEXCOPY log in PixelEngine::TextureCopy reports exactly one copy for this demo,\n    64x64, fmt=3 (IA8), source (0,0), destination 0x00239F40, a valid main-memory pointer, stride 16.\n    The working frb-copy demo copies 32x32 fmt=4 (RGB565) 354 times through the same code, so the copy\n    engine itself is fine.\n  - The light is a standard specular setup: GXInitSpecularDir + GXInitLightColor(0x88) +\n    GXInitLightAttn(-3, 0, 4, ...) with the comment \"Light equation 4x^2 - 3, x = N.H\". The emulator's\n    CosineAttenuation computes exactly clamp(a0 + a1*cos + a2*cos^2) = clamp(-3 + 4*cos^2), and the\n    diffuse material is white, so with the illumination term forced to 1.0 the hemisphere must render\n    white.\n  - Forcing the illumination term to 1.0 does NOT make the donut appear, so the lighting of the\n    hemisphere is not the fault: the hemisphere render, the copy and the texture the TEV samples do not\n    meet. The next thing to check is that the address the copy writes (PE_COPY_DST_BASE << 5, a physical\n    address) and the data pointer of the texture object the TEV samples (a pointer the emulator masks\n    itself) really are the same memory.\n\nDecisive check of the addressing: a temporary log of the texture engine's image address shows the\nlight map is programmed at addr=0x00239F40, fmt=3 (IA8), 64x64, 8192 bytes - exactly the destination\nthe copy wrote (0x00239F40, fmt=3, 64x64, stride 16). So the copy writes precisely where the sampler\nreads, and the light map is empty only because the EFB the copy read back held nothing.\nThe render that should fill it is drawHemisphere() (triangle strips over the whole screen with an\northographic projection, white diffuse material, the specular light above), and forcing the\nillumination term to 1.0 still left the picture black - which points at the ordering rather than at\nthe values: the GXCopyTex reads the EFB back, and the geometry the demo drew just before it has to be\nin the GL framebuffer by then. Next: check the emulator's GL draw batching/flush order around\nPE_COPY_CMD, i.e. whether ReadEfb sees the draws that the CP issued earlier in the same frame.\n\nFinal narrowing of this pass: the copy's readback *succeeds* - the TEXCOPY log line sits after the\nReadEfb() call and TextureCopy returns early when the readback fails - so the GL context is current,\nglReadPixels runs, and it returns black. The hemisphere the demo drew immediately before the copy is\ntherefore not in the framebuffer yet: the emulator clears/starts the GL frame on the draw path\n(GPFrameBegin -> GL_BeginFrame) while PE_COPY_CMD reads the EFB back, so the order of start-frame and\nreadback around the first frame is what to look at next (the demo does its one and only light map\nrender plus GXCopyTex *before* the main loop, i.e. before the first DEMOBeforeRender).\n\nAlso ruled out: the light map render targets the top-left 64x64 of the EFB\n(setupSize -> GXSetViewport(0,0,64,64) + GXSetScissor(0,0,64,64)), and TextureCopy converts that\nrectangle correctly for the GL readback (ReadEfb(srcX, RenderHeight - srcY - h, ...)), so the\nviewport placement is not the reason the copied rectangle is empty. That leaves the frame order:\nGPFrameBegin()/GL_BeginFrame() (which clears) runs from the draw path in cp.cpp, while PE_COPY_CMD\nreads the EFB back - for a copy made before the main loop's first frame the two have to be checked\nagainst each other.\n\nAnd the viewport mapping itself is right: GL_SetViewport does glViewport(x, scr_h - (h + y), w, h), i.e.\nit flips GX's top-left origin for GL, and RAS_End() issues glDrawArrays synchronously at GXEnd, so the\nhemisphere really is drawn into the GL buffer before the copy command is walked. What is left for this\ndemo is the frame bookkeeping (GPFrameBegin/GL_BeginFrame and the pending copy clears) around a\nPE_COPY_CMD that arrives before the main loop's first frame.\n\nOne more hypothesis ruled out: GL_SetScissor documents and implements the Y flip for the scissor\nrectangle (\"the programmed scissor rectangle is in screen coordinates, the origin is the top left\ncorner of the EFB ... GL measures its scissor box from the bottom left corner, so the Y axis is\nflipped here\"), so the 64x64 scissor and the 64x64 viewport agree with the rectangle the copy reads.\nWith the geometry, the lighting, the addressing, the viewport, the scissor and the GL draw order all\nchecked, what is left is the frame bookkeeping (GPFrameBegin/GL_BeginFrame plus the pending copy\nclears) around a PE_COPY_CMD that arrives before the main loop's first frame."
  },
  "gxdemo__tf-reflect": {
    "note": "FIXED by this sweep: the reflection demo renders its model now (170 colours; before it was a flat grey screen). It loads the position and texture matrices by index (GXLoadPosMtxIndx/GXLoadTexMtxIndx), which the emulator ignored.",
    "analysis": "Before: one colour (128,128,128). After: 170 colours, 46% dominant.\nSame root cause and fix as DL-tf-mtx (see that entry): the XF index-array block loads were not implemented."
  },
  "gxdemo__tev-outline": {
    "note": "Flat grey: the outline demo draws nothing. It is one of the two demos in the set that build a display list (GXCallDisplayList) and it also uses an indirect stage.",
    "analysis": "pe: 898, vi: 906, shot: one colour (160,160,160).\nSource: Tev/tev-outline.c.\n\nEvidence from the draw-command log (one run per demo):\n  - tev-outline submits 3402 primitives and 378 display-list calls in 3 s, so the model and the\n    display list do reach the command processor - the picture (one colour, the demo's own\n    clear colour 160,160,160) is not a missing draw, it is state: the model colour comes from\n    the per-vertex colour (GX_SRC_VTX) and the per-vertex position-matrix index, and the\n    enlarged black cartoon outline is missing too.\n  - ind-bump-xyz / ind-bump-st submit their donut (1900 GX_QUADS of 4 vertices plus strips) and\n    still come out pure black. Their textures come from gxTests/dnt-03.tpl and dnt-02.tpl, both\n    of which exist on the virtual disc, and the same TPL loading path works in tex-fmt-tpl.\n  - G2D-test submits a single draw per frame (GX_QUADS, 472 vertices = 118 quads, about 32/s),\n    which is the 2D layer batch; the captured picture is that layer drawn huge and dark\n    rather than the expected game scene.\nThese three are the next cases to work: they all have geometry, so the fault is in the state\nthat turns it into pixels (the per-vertex matrix index and the indirect/bump stage)."
  },
  "gxdemo__frb-zcopy": {
    "note": "Sparse content: black background with a few short white/yellow segments. The demo copies the EFB Z buffer into a texture and displays it, so the segments may be all that the Z-copy produced.",
    "analysis": "pe: 295 (well below vi: 956 - the frame counter only counts the GXDrawDone-based frames).\nSource: Framebuffer/frb-zcopy.c."
  },
  "gxdemo__lit-basic": {
    "note": "Three flat-shaded red shapes on grey. Correct: the demo is basic lighting control, and the shapes are lit with one light.",
    "analysis": "pe: 869. Two colours (grey background + red), which is what a flat-shaded lit object looks like."
  },
  "gxdemo__smp-octa": {
    "note": "Reference run: the rotating red/green octahedron from smp-octa.c, 932 frames in 7 s.",
    "analysis": "pe: 932, vi: 942."
  }
}