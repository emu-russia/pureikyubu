{
  "gxdemo__G2D-test": {
    "note": "FIXED by this sweep: the G2D scene (tiles, ship sprite, shadows) renders.",
    "analysis": "Source: G2D/G2D-test.c - the whole 2D layer is submitted as one quad batch per frame, and the batch is larger than 255 vertices, so the Peek16 truncation in src/cp.cpp made the command processor run it before its vertex data had arrived (see lit-atn-func). Before the fix the layer draw produced a flat dark shape; it is the full scene now."
  },
  "gxdemo__lit-atn-func": {
    "note": "FIXED by this sweep: the lit surface and its attenuation falloff render.",
    "analysis": "Source: Lighting/lit-atn-func.c - a half-cylinder lit by a spot light at its axis, with the attenuation function under test.\nTwo bugs:\n  - src/cp.cpp: FifoProcessor::Peek16 was declared to return a byte, so the 16-bit vertex count of a draw command was truncated to its low eight bits. This demo draws four strips of 362 vertices; enough-to-execute saw 106 and ran the command while most of its vertex data was still on its way in, so a quarter of the strip decoded as zeros. The stream buffer's back-pressure then had to be taught not to stop the reader in the middle of the command at the head (it stopped at 8 KB, less than the command needs), and the buffer is sized for the largest command a title can issue.\n  - src/xf.cpp: the vertex shader negated the light direction in the spot attenuation. The SDK stores the spot axis with the opposite sign, so the cosine is dot(L, Ldir) as it stands; the extra negation made every attenuation 0 and the surface came out black."
  },
  "gxdemo__tex-invalid": {
    "note": "FIXED by this sweep: the grid of ID textures and the INVALID/GXInvalidateTexAll markers render.",
    "analysis": "Source: Texture/tex-invalid.c - a grid of quads, one row per draw-sync token, with the texture region invalidated between rows. The picture is the whole test grid now (the draw-sync and the invalidation path were already right; the geometry of the rows was what the FIFO truncation ate)."
  },
  "gxdemo__mgt-fifo-brkpt": {
    "note": "FIFO break-point demo: it arms the CP break point and stops the FIFO on purpose, so the flat green screen and the missing frame counter are expected behaviour, not a failure.",
    "analysis": "pe finishes: 0, vi: 64 (the shortest run in the set).\nSource: Management/mgt-fifo-brkpt.c - the demo is about CP_FIFO_BRKL/BRKH and the CP status BRK bit (command-processor specs 4.2)."
  },
  "gxdemo__mgt-triple-buf": {
    "note": "FIXED by this sweep: the rotating cube renders (white and black faces on the blue clear).",
    "analysis": "Source: Management/mgt-triple-buf.c - a cube drawn per frame with the triple-buffer management on top. The cube used to be lost to the FIFO truncation (see lit-atn-func)."
  },
  "gxdemo__perf-geo": {
    "note": "A performance measurement demo (green screen). It is meant to be timed, not looked at, so the flat picture is not a rendering failure.",
    "analysis": "pe finishes: 0, vi: 775. CP 0x20: 0.\nSource: Performance/perf-geo.c - geometry throughput measurement."
  },
  "gxdemo__DL-tf-mtx": {
    "note": "FIXED further by this sweep: the display-list tube renders with its texture matrices.",
    "analysis": "Source: DisplayList/DL-tf-mtx.c - one matrix per contour loaded with GXLoadPosMtxIndx / GXLoadTexMtxIndx. The earlier sweep made the loaded register reach the XF at all; the byte-swap fix in LoadIndexedXF gives it the real matrix values, so the picture went from large flat areas to the drawn tube."
  },
  "gxdemo__frb-bound-box": {
    "note": "FIXED by this sweep: the eight cubes and the white bounding-box outline render.",
    "analysis": "Source: Framebuffer/frb-bound-box.c - the frame clears the box with GXClearBoundingBox, draws eight cubes, waits on a draw-sync token, reads the box back with GXReadBoundingBox and draws the outline from the two corners in screen space.\nThe PE bounding box was not emulated at all: PE_XBOUND / PE_YBOUND (0x55/0x56) latched the BP write and were never extended from the drawn geometry, and the CPU-visible PE_PI_XBOUND0/1 and YBOUND0/1 pair (0x10/0x12/0x14/0x16) read back as 0, so the outline collapsed to a point (gfx-pe.md 6.17).\nIt is extended now, once per primitive, with the window extent of the vertices the primitive is drawn from: the shader pipeline leaves the transform to the vertex program, so the rasterizer forms those window positions out of the same XF registers that program reads (the geometry matrix of the vertex's slot, the projection combine and the viewport scale/offset) and clamps them to the render target. A title clears the box with GXClearBoundingBox, which writes the empty range, so the two halves take care of each other with a minimum and a maximum per axis.\nThe box is the extent of the drawn vertices, not of the 2x2 quads the hardware walks, so a primitive that runs off the edge of the screen can leave the box a little larger than the pixels that were actually shaded."
  },
  "gxdemo__ind-bump-st": {
    "note": "FIXED by this sweep: the bump-mapped donut renders (light map and all). The four showMaps() overlay quads (the light map and the bump components in the screen corners) are still missing.",
    "analysis": "Source: Indirect/ind-bump-st.c - the emulator build takes the real indirect path (four TEV stages, one indirect stage, GXSetTevIndBumpST), so the donut is the material texture modulated by the light map indexed through the perturbed normal.\nRoot cause (fixed in src/gfx.cpp): the GL backend's own frame-begin clear took its depth from PE_COPY_CLEAR_Z, which a title only programs when it clears through the copy engine; on the first frame that register still holds 0, so the depth buffer was cleared to the near plane and every fragment of the frame failed the compare. The light map render is in exactly that frame.\nAlso fixed here (src/xf.cpp): the XF vertex shader handed the GX clip-space z straight to GL. The SDK matrices scale z into the GX range (-w, 0], so only half of the depth range was used and the programmed viewport z did not describe the depth GL was given; the range is doubled (z' = 2z + w) now, the same mapping the software pipeline applies in SoftVertexToWindow.\nStill open: the four showMaps() overlay quads (the light map and the bump components drawn in the screen corners) do not rasterise. Their vertices are in the VBO, the position attribute is bound and enabled, and a quad built from gl_VertexID at the very same clip positions does draw, while gl_Position taken from the position attribute does not; the cause is not culling, the scissor, the depth test, the alpha test or the near plane."
  },
  "gxdemo__ind-bump-xyz": {
    "note": "FIXED by this sweep: the indirect XYZ bump map renders. Same two root causes as ind-bump-st.",
    "analysis": "Source: Indirect/ind-bump-xyz.c - renderSetup2() calls GXSetTevIndBumpXYZ and the light map it indexes is generated the same way as in ind-bump-st (into the EFB, then GXCopyTex). The same first-frame depth clear and the same GX-to-GL z range fix make it draw."
  },
  "gxdemo__tf-reflect": {
    "note": "FIXED further by this sweep: the reflection model renders with its matrices.",
    "analysis": "Source: Texture/../tf-reflect.c - same indexed-matrix path as DL-tf-mtx, same byte-swap fix in src/cp.cpp."
  },
  "gxdemo__tev-outline": {
    "note": "FIXED by this sweep: the model and its cartoon outline render.",
    "analysis": "Source: Tev/tev-outline.c - the scene is drawn with per-object IDs in the alpha plane, the plane is copied out as an a8 texture with GXCopyTex and a screen-sized quad compares the ID map with a shifted copy of itself through the TEV and the alpha function.\nFour bugs, all fixed:\n  - src/cp.cpp: an indexed XF block load (GXLoadPosMtxIndx and friends) read the matrix out of the index array without byte-swapping it - the array is guest memory and every other word the CP reads through an array is swapped in FetchComp - so every per-ring matrix was garbage and the model was invisible.\n  - src/pe.cpp/h: the texture copy's destination format is the copy engine's own 4-bit set, not the texture unit's TexFormat. The single-channel formats (a8 and friends) are packed now, so the ID map is written at all.\n  - src/pe.cpp: the EFB read-back carried only the three colours; the single-channel copy formats need the alpha plane, so ReadEfb returns RGBA now and the SDL context asks for an alpha channel.\n  - src/tev.cpp: an unclamped stage keeps an 11-bit signed result, but what the alpha function compares is the low 8 bits of the completed alpha. The demo subtracts two texture lookups and relies on the two's complement wrap to see the same edge from either side."
  },
  "gxdemo__frb-zcopy": {
    "note": "Sparse content: black background with a few short white/yellow segments. The demo copies the EFB Z buffer into a texture and displays it, so the segments may be all that the Z-copy produced.",
    "analysis": "pe: 295 (well below vi: 956 - the frame counter only counts the GXDrawDone-based frames).\nSource: Framebuffer/frb-zcopy.c."
  },
  "gxdemo__lit-basic": {
    "note": "Three flat-shaded red shapes on grey. Correct: the demo is basic lighting control, and the shapes are lit with one light.",
    "analysis": "pe: 869. Two colours (grey background + red), which is what a flat-shaded lit object looks like."
  },
  "gxdemo__smp-octa": {
    "note": "Reference run: the rotating red/green octahedron from smp-octa.c, 932 frames in 7 s.",
    "analysis": "pe: 932, vi: 942."
  }
}