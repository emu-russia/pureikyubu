// TODO: The settings dialog is tied to the hardware emulation of memory cards (HW/MC.cpp).
// This is not good, you need to make the memory card settings dialog only work with the configuration.

/*
 * Calls the memcard settings dialog
 */ 
void MemcardConfigure(int num, HWND hParent);
